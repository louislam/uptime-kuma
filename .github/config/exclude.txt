# This is a .gitignore style file for 'GrantBirki/json-yaml-validate' Action workflow

# Exclude Helm templates as they contain Go template syntax that isn't valid YAML until rendered
helm/templates/
helm/templates/**