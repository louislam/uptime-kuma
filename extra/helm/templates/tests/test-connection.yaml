apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uk.fullname" . }}-test-connection"
  labels:
    {{- include "uk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
